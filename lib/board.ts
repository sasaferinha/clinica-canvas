export type Card={id:string;type:'text';x:number;y:number;width:number;height:number;text:string;color?:string};
export type Edge={id:string;fromNode:string;toNode:string;fromSide?:string;toSide?:string;label?:string;toEnd?:string};
export type Board={title:string;nodes:Card[];edges:Edge[]};
export const colors:Record<string,string>={'1':'#ef8888','2':'#e7ad73','3':'#e0ca76','4':'#70c6a1','5':'#70b8db','6':'#b49aef'};
const data=[['central',460,330,'6','Projeto da clínica','Organizar o marketing antes de produzir conteúdo.\n\nDefinir prioridades, responsabilidades e próximos passos.'],['diagnostico',30,30,'5','Diagnóstico','Nome e cidade da clínica\nServiços e público atendido\nPresença digital atual\nCapacidade para novos pacientes'],['objetivos',460,0,'6','Objetivos e prioridades','Escolher o atendimento prioritário\nDefinir o resultado esperado\nCombinar metas realistas'],['acordo',890,30,'2','Escopo e responsabilidades','Definir suas entregas\nQuem aprova os materiais?\nQuem responde ao WhatsApp?\nCombinar orçamento e início'],['perfis',30,330,'4','Perfis e vídeos','Pessoal: experiência e rotina\nClínica: equipe e serviços\nVolume e gravações a combinar\nPautas entram na próxima etapa'],['site',890,330,'5','Site da clínica','Serviços, equipe e localização\nContato para agendamento\nReunir logo e fotos reais\nDefinir criação e manutenção'],['anuncios',30,650,'2','Tráfego pago','Serviço e região prioritários\nVerba separada da remuneração\nDestino e recepção preparados\nAprovação antes do lançamento'],['rotina',460,680,'4','Etapas do projeto','1. Diagnóstico e acordo\n2. Organizar acessos e materiais\n3. Implantar as frentes aprovadas\n4. Avaliar e ajustar'],['resultados',890,650,'6','Acompanhamento','Investimento por canal\nContatos e agendamentos\nReunião mensal de análise\nDecisões para o mês seguinte']];
export const initialBoard:Board={title:'Marketing da clínica',nodes:data.map(([id,x,y,color,title,body])=>({id:String(id),type:'text',x:Number(x),y:Number(y),width:320,height:240,color:String(color),text:`# ${title}\n\n${body}`})),edges:data.slice(1).map((a,i)=>({id:'edge'+i,fromNode:'central',toNode:String(a[0]),toEnd:'arrow'}))};
export function validateBoard(raw:unknown):Board {const b=raw as Board;if(!b||!Array.isArray(b.nodes)||!Array.isArray(b.edges)||b.nodes.length>300||b.edges.length>1000)throw Error('Mapa inválido. Limite: 300 cartões e 1.000 conexões.');const ids=new Set<string>();const nodes=b.nodes.map(n=>{if(n.type!=='text')throw Error('Esta versão importa cartões de texto. Arquivos, links e grupos não são suportados.');if(typeof n.id!=='string'||!n.id||ids.has(n.id)||typeof n.text!=='string'||n.text.length>20000||![n.x,n.y,n.width,n.height].every(v=>Number.isFinite(v)&&Math.abs(v)<100000)||n.width<120||n.height<80)throw Error('Cartão inválido.');ids.add(n.id);return {...n,x:Math.round(n.x),y:Math.round(n.y),width:Math.round(n.width),height:Math.round(n.height)};});const eids=new Set();const edges=b.edges.map(e=>{if(typeof e.id!=='string'||eids.has(e.id)||!ids.has(e.fromNode)||!ids.has(e.toNode)||e.fromNode===e.toNode)throw Error('Conexão inválida.');eids.add(e.id);return e;});return {title:typeof b.title==='string'?b.title.slice(0,100):'Mapa importado',nodes,edges};}
